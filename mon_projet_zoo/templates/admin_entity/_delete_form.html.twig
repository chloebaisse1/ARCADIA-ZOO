<form method="post" action="{{ path('app_admin_entity_delete', {'id': admin_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin_entity.id) }}">
    <button class="btn">Delete</button>
</form>
